//Question 68
/* Multiplying Decimals: Create a function that accepts two decimal
 numbers and returns their product. Round the result to two
 decimal places.*/
function decimalsMultiplication(num1, num2) {
    return Math.round(num1 * num2);
}
//Trying it with 1.1 and 1.2
console.log(decimalsMultiplication(1.1, 1.2)); //output 1.32
//After multiplying we roune the result that is 1
