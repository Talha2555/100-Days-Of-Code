//            Question 76
/* Function Parameters and Return Values: Create a function that takes two
 numbers as parameters, adds them together, and returns the result. Show how 
 you can call this function and log the result. */
function myFunc(num1: number, num2: number): number {
  return num1 + num2;
}
//calling the function with 2 numbers and logging the result
console.log(myFunc(6, 7)); //output 13
